import React, { useState } from "react";
import "./Footer.css";

const Header = () => {

    return (
        <footer>
            <p>Built with React, Typescript, CSS, Material UI, HTML</p>
        </footer>
    )
}

export default Header